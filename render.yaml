services:
  - type: web
    name: mining-mobility
    runtime: python
    buildCommand: pip install -r requeriments.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
